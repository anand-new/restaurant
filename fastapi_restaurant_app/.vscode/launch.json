{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Uvicorn App",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "app.main:app",       // <- replace with your actual entry point
        "--host", "127.0.0.1",
        "--port", "8000",
        "--log-level", "debug"
      ],
      "justMyCode": true
    }
  ]
}
